#include <bits/stdc++.h>

using namespace std;

int main() {
  int   a,b,z,x,y;
  cin>>a>>b>>x>>y>>z;
  if((a>x and b>y) or (a>z and b>y) or (a>z and b>z) or (a>x and b>z) or (a>y and x<b)){
      cout<<1;
  }
  else{
      cout<<0;
  }
}
