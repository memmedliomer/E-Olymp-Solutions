#include <bits/stdc++.h>
#include <cmath>
using namespace std;

int main()
{
    long long n;
    double cem=0,hasil=1,b;
    cin>>n;
    while(n>0){
        b=n%10;
        n=n/10;
        cem=b+cem;
        hasil=hasil*b;
    }

    cout<<fixed<<setprecision(3)<<hasil/cem;
    }
