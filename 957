#include <bits/stdc++.h>

using namespace std;
int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	double a,b,c,d;
	int n;
	cin>>n;
	a=n%10;
	b=n/10%10;
	c=n/100;
	d=sqrt(a+b+c);
	cout<<fixed<<setprecision(3)<<d;
}

