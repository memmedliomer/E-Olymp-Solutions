#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n,a,b;
    cin>>n;
    
    cin >> b;
    
    for(int i=0; i<n-1; i++)
    {
        int el;
        
        cin >> el;
        
        b = __gcd(b,el);
        
    }
    
    cout << b << endl;
    
}
