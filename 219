#include <bits/stdc++.h>

using namespace std;

int main() {
   long long a,b,c,d,e,f;
   cin>>a>>b>>c>>d;
   if((a*b*c)%d==0){
       cout<<(a*b*c)/d;
   }
   else{
       cout<<((a*b*c)/d)+1;
   }
}
