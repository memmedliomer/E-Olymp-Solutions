#include <bits/stdc++.h>
using namespace std; 
int main() { 
 string s;
 long long say=0;
 getline(cin,s);
 for(int i=1; i<s.size(); i++){
     if(s[i]=='+' or s[i]=='-' or s[i]=='*'){
         say++;
     }
 }
cout<<say;
}
