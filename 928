#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v1;
    long long  n,b,a,r=0,el,maxel,minel,d,w,j=0;
    cin>>n;
    b=n;
    
    for(int i=0; i<n; i++){
        cin>>el;
        v1.push_back(el);
        
    }
 w=*max_element(v1.begin(),v1.end());
 a=*min_element(v1.begin(),v1.end());
 cout<<w+a<<endl;
    }
