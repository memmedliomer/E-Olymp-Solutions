#include <bits/stdc++.h>
using namespace std; 
int main() { 
 long long n,cem=0;
 cin>>n;
 n=abs(n);
 vector<long> v;
 for(int i=0; n>0; i++){
     long long d=n%10;
     v.push_back(d);
     n=n/10;
 }
 for(int i=0; i<v.size(); i++){
     cem=cem+v[i];
 }

    cout<<cem;
}
