#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n;
    double el, max = -101;
    cin >> n;
    
    while(n--)
    {
        cin >> el;
        el = abs(el);
        if(el > max){
            max = el;
            }
    }
   

cout <<fixed << setprecision(2) <<  max;
}
