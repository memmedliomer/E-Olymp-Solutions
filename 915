#include <bits/stdc++.h>

using namespace std;

int main()
{
   int  a,b,c;
   cin>>a>>b>>c;
   if(((c*c)==(a*a)+(b*b))     or    (b*b)==(a*a)+(c*c)   or ((a*a)==(c*c)+(b*b))){
    cout<<"YES"<<endl;
   }
   else{
       cout<<"NO"<<endl;
   }
}
