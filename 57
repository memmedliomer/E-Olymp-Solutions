#include <bits/stdc++.h>
using namespace std;
int main() {
  double x1, y1, z1, x, y, d;
    cin >> x >> y >> x1 >> y1 >> z1;
    d = sqrt((x1 - x) * (x1 - x) + (y1 - y) * (y1 - y) + z1 * z1);
    d = 1 / d;
    cout<<fixed<<setprecision(3)<<d;
 }
